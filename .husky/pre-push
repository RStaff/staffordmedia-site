#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "=== PRE-PUSH CHECK (NON-BLOCKING) ==="

npm run check || echo "⚠️ check failed (non-blocking)"
npm run test || echo "⚠️ test failed (non-blocking)"

echo "✅ allowing push (CI is source of truth)"
exit 0
