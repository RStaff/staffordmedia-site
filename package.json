{
  "name": "staffordmedia-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root wrapper so Render can build the app in _smc_clean",
  "scripts": {
    "build": "node scripts/render-root-build.js"
  }
}
