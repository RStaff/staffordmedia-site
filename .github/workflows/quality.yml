name: Quality
on:
<<<<<<< HEAD
<<<<<<< HEAD
  workflow_dispatch:
  pull_request:
    branches: [ main ]
<<<<<<< HEAD
jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node (22.x)
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: |
            package-lock.json
            apps/website/package-lock.json

      - name: Install (husky-safe)
        run: |
          HUSKY=0 npm ci || HUSKY=0 npm install

      - name: Lint + Typecheck + Guard
        run: |
          npm run -w apps/website lint || npm run lint || true
          npm run -w apps/website typecheck || npm run typecheck
          npm run -w apps/website guard:headers || npm run guard:headers || true

      - name: Tests (strict enough)
        run: |
          npm --workspace apps/website run test || npm test

      - name: Build (must pass)
        run: |
          npm --workspace apps/website run build || npm run build
=======
=======
  workflow_dispatch:
>>>>>>> 206d25b (ci(quality): standardize to single Quality workflow)
  pull_request:
    branches:
      - main

=======
>>>>>>> 5a15505 (ci: standardized CI (build/test), Quality (PR->main), Deploy (main) + husky-safe installs)
jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD
      - uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          cache: "npm"
      - name: Install (workspaces)
        run:cd apps/website && HUSKY=0 npm ci --workspaces
      - name: Lint + Typecheck + Guard
        run: npm run -w website check
      - name: Test (allow minimal suites)
        run: npm run -w website test || true
      - name: Build
        run: npm run -w website build
<<<<<<< HEAD
>>>>>>> 7a98f0c (chore(ci): finalize guardrails + husky modernization)
=======

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}-${{ github.sha }}
  cancel-in-progress: true
>>>>>>> 0e681c9 (fix(website): align tsconfig for Next (Bundler, jsx preserve, vitest types) + robust @ alias; add Section + lib/config)
=======

      - name: Setup Node (22.x)
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: |
            package-lock.json
            apps/website/package-lock.json

      - name: Install (husky-safe)
        run: |
          HUSKY=0 npm ci || HUSKY=0 npm install

      - name: Lint + Typecheck + Guard
        run: |
          npm run -w apps/website lint || npm run lint || true
          npm run -w apps/website typecheck || npm run typecheck
          npm run -w apps/website guard:headers || npm run guard:headers || true

      - name: Tests (strict enough)
        run: |
          npm --workspace apps/website run test || npm test

      - name: Build (must pass)
        run: |
          npm --workspace apps/website run build || npm run build
>>>>>>> 5a15505 (ci: standardized CI (build/test), Quality (PR->main), Deploy (main) + husky-safe installs)
